# MLE Hiring Challenge — Dependencies
# Core: Python 3.9+

# LLM Providers (install both, agent uses whichever key is available)
anthropic>=0.40.0
groq>=0.9.0

# AWS Bedrock support (for Anthropic Bedrock bearer tokens)
boto3>=1.34.0

# LSA embeddings (Latent Semantic Analysis via SVD)
numpy>=1.24.0

# Environment variable loading (optional — main.py has built-in .env parser)
python-dotenv>=1.0.0
